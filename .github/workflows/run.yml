name: run

on:
  push:
      branches:
            - main
jobs:
  build:
    runs-on: ubuntu-lastest
    steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-node@v2
          with:
          node-version: 18.12.1
          -run: home.html
       
